{
  "name": "fix-my-shot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Off-court basketball shot-form trainer — grade an editable, physically-real shooting pose against research-derived principles.",
  "license": "MIT",
  "author": "Adar",
  "engines": {
    "node": ">=22.13"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @fix-my-shot/web",
    "build": "npm run build --workspace @fix-my-shot/web",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "spike:measure": "npm run build --workspace @fix-my-shot/web && node tools/spike-measure.mjs",
    "posegen": "python3 tools/posegen/generate.py",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "setup": "bin/setup"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.10.1",
    "@vitejs/plugin-react": "^5.2.0",
    "eslint": "^10.8.0",
    "globals": "^17.7.0",
    "prettier": "^3.4.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.16.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
