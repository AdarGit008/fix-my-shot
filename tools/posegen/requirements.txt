# Offline pose pipeline (ADR-0003 / ADR-0007). Pinned for reproducible generation.
# The generator's gate uses classic MuJoCo forward kinematics (reliable contacts/COM);
# mujoco-mjx + jax are the ADR-0003 MJX-offline toolchain, pinned here for the
# differentiable leverage oracle that lands with issue #12.
mujoco==3.10.0
numpy==2.5.1
mujoco-mjx==3.10.0
jax==0.11.0
jaxlib==0.11.0
