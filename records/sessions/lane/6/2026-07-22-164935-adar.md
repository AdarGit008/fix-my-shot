---
record: session/1
lane: lane/6
agent: adar
started: 2026-07-22T16:49:35Z
---

# Session — lane/6 — 2026-07-22

## Did

Built the issue #6 engine spike and got a **GO**. Stood up the official `@mujoco/mujoco` single-threaded WASM build (no COOP/COEP needed) + a three.js primitive renderer in `apps/web` behind `?spike`: one mesh per MuJoCo geom, world transforms synced from `geom_xpos`/`geom_xmat` each frame, model under a −90°X root (z-up→y-up, zalo/mujoco_wasm convention). Vendored DeepMind's canonical `humanoid.xml` (Apache-2.0, self-contained — no includes/meshes) and added a basketball freejoint. Embind `.delete()` lifecycle registry from day one (`handles.ts`); DragStateManager-lite via `mjv_applyPerturbForce` (`drag.ts`); self-measuring page + a headless Playwright harness (`tools/spike-measure.mjs`, `npm run spike:measure`) that drives the production build.

Measured on the production build, all three §11.7 budgets cleared by 25×–500×: re-grade+report **p95 0.2 ms** (mj_forward on nq35/21geoms), drag **≥60 fps observed / ~2500 fps CPU capacity**, initial load **~0.22 s**. Verified the scene renders correctly by screenshot (upright humanoid holding the ball, floor horizontal). Recorded numbers into SPEC §11.7 (criterion 7) + §9, ADR-0003's benchmark consequence, and a new go/no-go record `docs/spikes/0006-engine-benchmark.md` (+ committed `results.json` + screenshot). The Rapier/Jolt fallback named in the issue is not needed.

Deps added to `apps/web`: `three@^0.185`, `@mujoco/mujoco@^3.10` (both ADR-0007-named), and `playwright` devDep + `spike:measure` script. Green across typecheck · lint · 24 tests · format:check · build. (`*.md`/`docs/`/`records/` are prettier-ignored per REC-01, so prose edits are hand-authored.)

## Left open

next: open the lane/6 → main PR (forge reachable via `unset GH_TOKEN`; needs a demo GIF/screenshot in the PR body). Residual from §11.7: confirm the budget on a *defined* mid-range reference — margins swamp any plausible gap, but the caveat stands. Founder call: keep `playwright` as a committed devDep, or defer browser tooling to #15's run/verify skill. The spike is perf+integration only — grip/pose correctness, the validity gate, IK editing, and the scorer are later issues (#7–#14) toward the v0.1 local-deploy milestone (#15).
