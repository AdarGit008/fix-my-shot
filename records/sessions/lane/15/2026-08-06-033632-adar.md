---
record: session/1
lane: lane/15
agent: adar
started: 2026-08-06T03:36:32Z
---

# Session — lane/15 — 2026-08-06

## Did

Closed out **issue #15 — local deploy**, the v0.1 finish line. This lane was
claimed *after* the session first landed the whole v0.1 stack bottom-up
(#39→#43 merged into `main`; GitHub's auto-retarget mis-fired once and closed
#40, repaired by restoring `lane/10`, reopening, retargeting via REST; every
later step retargeted manually before branch deletion) and swept the
dependabot queue (#44 minor group merged; #37 `@types/node` 26 closed via
`@dependabot ignore this major version` — runtime pins Node 22; #28 TS 6.0.3
closed as superseded by #35). So everything below was validated against the
*merged* milestone plus fresh deps (vite 8.2 / globals 17.9 / playwright
1.62.1).

**Deploy-readiness, proven not asserted:** `npm run verify:loop` on the
production bundle — 9/9 steps PASS (boot → grade-on-load → faulted pose →
accepted edit → reload continuity → history; zero page/console errors).
`npm run spike:measure` re-confirmed the §11.7 budget on the deployed build:
initial load **309 ms** ≤ 5 s, re-grade+report **avg 0.066 / p95 0.2 ms**
≤ 100 ms, drag **60 fps observed** (~1430 fps CPU capacity) ≥ 30 fps —
**VERDICT: GO**. Residual unchanged from #6: dev-machine numbers, not the
defined mid-range reference; margins (16×–500×) swamp the gap.

**COOP/COEP resolved by inspection:** the `@mujoco/mujoco` build in use is
single-threaded (no SharedArrayBuffer) — the whole question dissolves; any
static host serves `apps/web/dist` unmodified. Recorded in README + skill so
nobody re-litigates it.

**The committed run skill** (`.claude/skills/run/SKILL.md`): launch (dev vs
built bundle), the four routes, how to drive the loop (⚠ faulted poses,
drag→verdict→re-grade, engagement semantics), the two headless harnesses with
their ports/gotchas (strict ports 4188/4190, SwiftShader floor, one-time
`npx playwright install chromium`). Bootstraps the built-in run/verify
patterns for any future agent session.

**Demo media** (`docs/media/`): a 9 s GIF of the real loop (boot → faulted
pose → accepted edit → history) + four stills, captured by a Playwright drive
of the production build. Toolchain note for posterity: Playwright's bundled
ffmpeg has no `fps` filter and no GIF encoder — frames were extracted with
`-r 4 -vf scale` and assembled via `gifenc` (760 px, 38 frames, 1.58 MB).

**README refresh:** status header now says v0.1 is live and done; "Getting
started" runs the real thing (run → verify → develop → orient), embeds the
GIF, and links the skill. CHANGELOG entry added. Issue #15 checklist maps:
dev/build ✓ (verify:loop builds + drives), run skill ✓, README ✓, media ✓,
perf ✓ (GO, numbers above).

## Left open

next: v0.1 closes when this PR merges — cut the v0.2 planning pass (candidate
  threads: the #6 mid-range-reference perf check, MJX leverage oracle once the
  TS scorer is re-expressed in JAX, wrist/finger principles pending a model
  with wrist DOF, camera preset framing body+rim together).
- `@types/node` majors are suppressed on the dependabot side until the runtime
  moves off Node 22 — revisit `.nvmrc`/`engines` together when it does.
- Vite 8.2 (rolldown) warns about >500 kB chunks on build (the engine bundle);
  harmless for a local static deploy, a code-split candidate if load budget
  ever tightens.
