---
record: session/1
lane: lane/9
agent: adar
started: 2026-07-28T19:20:00Z
---

# Session — lane/9 — 2026-07-28

## Did
Built issue #9 — the physical-validity gate: a TypeScript port of kevinzakka/mink's QP formulation (pinned 44c8a6ab66d27d06249f9018334a51662605e3e4, Apache-2.0 attribution headers in every ported file) against the `@mujoco/mujoco` WASM API, in `apps/web/src/gate/`. Green: typecheck, lint, 122 gate tests (+ all pre-existing), Vite build.

Layer 1 — the authority (`check.ts` + `engine.ts`): static three-check classifier in lockstep with `tools/posegen/gate.py` (joint limits ±0.5°, COM-in-support over the foot-capsule-endpoint hull with world subtree COM incl. ball, penetration ≤ 5e-3 m inclusive over ALL contacts). Acceptance test: all 33 shipped library poses pass from stored qpos; max penetration delta vs the Python gate 5.8e-7; tamper tests flip each check individually (incl. the physically-honest COM tamper — root translation can't break balance, torso lean does).

Layer 2 — the mink port: `lie.ts` (SO3/SE3, translation-first tangents), `configuration.ts` (kinematics + body-frame jacobians via mj_jacBody/Geom + adjoint, mj_integratePos integration), `tasks.ts` (Frame/Posture/Com; H += WJᵀWJ + μI, c += −WeᵀWJ, μ counted once — fused-equivalence tested), `limits.ts` (ConfigurationLimit, VelocityLimit, CollisionAvoidanceLimit bit-for-bit incl. upstream's 1/dt velocity-damper quirk — documented; plus ComSupportLimit, OUR addition: hull-edge inequalities on the projected COM), `solve.ts` (QP in Δq, v = Δq/dt), `qp.ts` (Goldfarb–Idnani dual active-set — the one genuinely new piece; KKT-verified, cross-checked against mju_boxQP). `index.ts`: `createGate` facade — project(drag) with feet-anchor + posture defaults, authority re-check, reject-and-revert (invalid input ⇒ caller gets its qpos back byte-for-byte).

Oracles (the issue's "validate against mink's test suite" DoD, upgraded to cross-language golden parity): user-approved executing pinned mink + mujoco 3.10.0 in a scratch venv (python 3.13 via uv) — mink's own suite 112/112 passed; committed `oracles/*.json` fixtures generated from THIS scene.xml + library poses recording e/J per task, assembled H/c/G/h, daqp's v, integrated q_next (byte-deterministic across runs). TS parity: lie 1e-9; errors 1e-9; jacobians 1e-8; QP 1e-8; solved step (GI vs daqp) 1e-6; q_next 1e-8. Structural ports: FD jacobians (√eps step survives WASM), unit-cost identities, LM identity, dt-invariance, convergence (‖e‖ → 1e-13), and the gate property: a 50-step 0.5 m hand projection where EVERY intermediate qpos passes the authority.

Also closed an #8 caveat: `tools/posegen/generate.py --verify` finally ran (venv mujoco) — verified 33 poses across 5 phases, 0 invalid, exit 0. Pre-lane housekeeping same session: dependabot batch PR #25 merged (react 19 pair, globals 17, plugin-react 5, dependabot ignores for TS 7.0.x / plugin-react ≥6), #18–#22 closed, lane/6/7/8 remote branches pruned.

Engine findings future lanes need: mjtByte arrays (`model.jnt_limited`, `data.eq_active`) throw BindingError — use accessors or jnt_range; typed-array views silently detach after ANY wasm heap growth — re-acquire per call; `data.contact` has copy semantics — delete vec + elements; README's `.getView()` is wrong (capital `GetView()`); scene has no `<site>` elements — frame tasks target bodies/geoms.

Scope / dead ends: self-collision pairs beyond drag↔floor not wired into the default facade (primitives support any pair list); wrist/finger DOF still absent (unchanged from #8); ε and slack constants stay gate.py's verbatim (5e-3 / 0.5°) — changing them means regenerating the library; mink's CollisionAvoidanceLimit h scales 1/dt in a Δq QP (looser than a displacement damper for dt < 1) — matched bit-for-bit for oracle fidelity rather than "fixed".

## Left open
next: Open + merge the lane/9 → main PR; then build #10 (pose editor: bounded per-phase dragging behind the gate) consuming `createGate`/`project` — the facade's extraTasks/extraLimits hooks are the intended seam, and the per-phase parameter bounds from ADR-0009 still need transcribing into editor clamps before the gate runs.
