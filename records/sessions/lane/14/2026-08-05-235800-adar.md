---
record: session/1
lane: lane/14
agent: adar
started: 2026-08-05T23:58:00Z
---

# Session — lane/14 — 2026-08-06

## Did

Built issue #14 — the core loop assembled in the app shell, stacked on lane/13.
Green: lint, typecheck, 280 tests repo-wide, Vite build, and the new
end-to-end verify flow (9/9 steps).

**The shell** (`App.tsx` + `EditorPage.tsx` evolved into the loop): the DEFAULT
route is the product — generate (library pose by phase; the virtual target now
rendered as a rim ring + stanchion at the ADR-0009 free-throw geometry, a
direction reference nothing travels to) → edit (unchanged #10 machinery) →
re-grade on every accepted edit (buildReport, ~13 ms) → the ranked report
(`ReportPanel`: grade with not-a-probability framing, fixes in external-focus
vocabulary with the top fix leading, per-principle chips: ✗ violated / ⚑ style
flag / ✓ in range / ∅ unmeasurable-on-this-model) → progress (#13's store).
`?editor` stays as an alias; `?spike` and `?history` remain deep-linkable.
Copy audited for scaffold framing (ADR-0005, acceptance #8): "an off-court
scaffold for your shooting eye … alongside — never instead of — real
practice"; no efficacy/transfer claim anywhere.

**Engagement-based recording** — the find of the lane, surfaced by the verify
flow, not by unit tests: recording a sitting on POSE LOAD meant a reload (which
loads the default clean pose) inserted a fix-less sitting between two real
ones, wiping continuity. Semantics now: a sitting records only once the user
ENGAGES (first accepted edit; upserts thereafter), and the loop page's live
continuity line uses the new `continuityAgainstReport` — the last STORED
sitting's top fix compared against the LIVE report (improved / still working /
not-re-measured), excluding the current sitting's own entry. The ?history
view's stored-only `topFixContinuity` is unchanged (#13 tests stand); the new
helper has its own unit tests.

**Verify flow** (`npm run verify:loop`, `tools/loop-verify.mjs`, committed —
the issue's "the verify flow drives it end to end"): serves the production
build, drives TWO sittings headless — boot on the default route → report
renders on load → faulted pose reports ranked fixes (grade 75 on the dip
fault) → grid-drag until the gate accepts → re-grade → reload → the second
sitting shows the prior top fix from localStorage → second sitting edits →
?history lists both sittings → zero console errors. 9 steps, exit-code
gated, spike-measure conventions (vite preview + SwiftShader).

**SPEC §11 acceptance criteria, run as the issue's checklist:**
1. Generated pose (any phase) → form score, no trajectory/make-miss/aim — PASS
   (buildReport everywhere; no flight computation exists; the target is
   non-physical and now visibly a reference).
2. Every editable pose passes the validity gate before scoring — PASS (the
   loop grades only committed poses; commits exist only via projection +
   authority accept with reject-and-revert; #10's suite).
3. In-range style never penalized — PASS (library-wide test; style flags
   deduct zero and never become fixes).
4. Backspin from hand/ball placement + touch points, never flight — PASS (the
   produced-backspin proxies read static geometry; spin rate nowhere).
5. All fixes ranked; top fix stable under small perturbations — PASS (#12's
   stability sweep across every faulted pose).
6. External-focus phrasing — PASS (cue table + anatomy-blacklist test; 4
   sanctioned rows recorded per ADR-0009).
7. Deterministic re-grade within budget — PASS (determinism tests; report
   p50 12.7 ms ≤ 100 ms; drag preview ~1 ms/frame on top of #6's 25×–500×
   margins). Residual: numbers are from this dev machine — the defined
   mid-range reference check remains #6's open residual, unchanged.
8. Transfer/efficacy claims gated; scaffold framing — PASS (copy audit above;
   no claim to gate yet).
9. Sport logic behind plugin interfaces; core names no sport — PASS
   (core-seam guard + lint boundary, still green with all new code).

## Left open

next: #15 (local deploy) is the milestone's finish line — README status +
  getting-started refresh, a committed run skill bootstrapping verify:loop,
  demo GIF/screenshots, and the perf budget re-confirmed on the deployed
  build. Everything it needs (build, verify flow, screenshots) exists on this
  stack.
- The rim renders at the nominal free-throw spot; a camera preset that frames
  body AND rim together would help first-time orientation (pure polish).
- Multi-fault sittings record the report of the LAST accepted edit (upsert) —
  per-edit deltas inside one sitting are post-v1 progression material.
