# fix-my-shot environment template (BUILD-04).
#
# No runtime secrets are required yet — v1 is fully client-side and localStorage-only
# (ADR-0007). Enumerate variables here (names only, never real values) as they land.
# Vite exposes only variables prefixed VITE_ to client code.
#
# Example:
# VITE_FEATURE_FLAG=
